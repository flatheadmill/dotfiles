# Remote session overrides. Both bars at the bottom — remote stacks above local.

set -g status-position bottom

# Remote tunnel port for clipboard over SSH.
set -g @copy_backend_remote_tunnel_port 11988

# Remote status-right — date, no host.
set -g status-right "$wg_is_keys_off$wg_is_zoomed $wg_date "
