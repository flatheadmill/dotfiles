### Issue by Issue

 * Rename `dots travis open` to `dots travis url` #36.
 * Rename `dots git open` to `dots git url` #35.
 * Print Circle CI url from Node.js project. #34.
 * Print Travis CI url from Node.js project. #33.
